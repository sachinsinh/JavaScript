// Rules for naming variable

// 1)- we cannot start with number 

    // 1variable (Invalid)
    // variable1 (valid)

    var variable1 = "10";
    console.log(variable1);

// we can only use underscore _ and $ dollar variable for naming

    // _firstName (valid)
    // first_Name (valid)

    var first_Name = "mohit"; //snake case writing
    var firstName = "sachin"; //camel case writing
    console.log(first_Name);
    console.log(firstName)

    // $firstName (valid)
    // first$Name (valid)

//we cannot use space in naming

    // first name (Invalid)

    // var first name = "mohit"; (error)
    // console.log(first name);

// 2)-convention

    // start with small letter and use camelcase 
    
